{
  "directive": "Create the initial architecture document with package layout, error definitions, constants, and the EventType enum.\n\n**Package Layout (Checklist Item 1)**\n\nDefine the project structure showing:\n```\nstarter_golang_aws_lambda/\n├── cmd/\n│   └── lambda/\n│       └── main.go\n├── internal/\n│   ├── handlers/\n│   │   ├── apigateway.go\n│   │   ├── customevent.go\n│   │   └── stepfunction.go\n│   └── router/\n│       └── router.go\n├── tests/\n│   ├── unit/\n│   └── e2e/\n├── terraform/\n│   ├── main.tf\n│   ├── variables.tf\n│   ├── outputs.tf\n│   ├── api_gateway.tf\n│   ├── step_functions.tf\n│   └── lambda.tf\n└── scripts/\n    ├── deploy.sh\n    ├── destroy.sh\n    └── test-e2e.sh\n```\n\n**Error Sentinel Variables (Checklist Items 2-4)**\n\nIn `internal/handlers/errors.go`, define:\n- `ErrInvalidEvent` — returned when unable to parse event\n- `ErrUnsupportedRoute` — returned for unsupported API routes\n- `ErrIterationFailed` — returned when step function iteration fails\n\nEach should use `errors.New()` with 'handler: ' prefix per the design doc.\n\n**Constants (Checklist Item 19)**\n\nIn `internal/handlers/constants.go` or within relevant handler files, define:\n- `DefaultAWSRegion` = \"us-east-1\"\n- `DefaultIterations` = 5\n- `DefaultDelayMs` = 1000\n\n**EventType Enum (Checklist Item 7)**\n\nIn `internal/router/types.go`, define:\n- `EventType` as a string type or int type\n- Constants: `EventTypeAPIGateway`, `EventTypeStepFunction`, `EventTypeCustomEvent`\n\n**Import Requirements**\n\nInclude the standard library imports needed:\n- `errors` for sentinel errors\n- Other imports will be added in subsequent iterations\n\n**Package Documentation (Checklist Items 64-65)**\n\nAdd package-level doc comments:\n- For `package handlers`: Describe Lambda event handler responsibilities\n- For `package router`: Describe event detection and routing responsibilities\n\n**Build Requirements (Checklist Item 20)**\n\nNote in the document that the project requires Go 1.23+ (this will be reflected in go.mod).\n\nOutput the complete `architecture.md` file with these foundational elements.",
  "complete": false
}
